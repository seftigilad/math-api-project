# IO.Swagger - ASP.NET Core 2.0 Server

No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)

## Run

Linux/OS X:

```
sh build.sh
```

Windows:

```
build.bat
```

## Run in Docker

```
cd src/IO.Swagger
docker build -t io.swagger .
docker run -p 5000:5000 io.swagger
```

## 🔐 JWT Authentication

To enable JWT authentication, configure your JWT secret in `appsettings.json` or directly in `Startup.cs` like:

````csharp
var key = Encoding.ASCII.GetBytes("YourSuperSecretKeyHere!");
secrert now is hard coded


string jwtSecret = "P#nM7!sD8uA^2yJkXqL0*Tf9ZvR4%WqE";

Make sure your payload includes:
{
  "sub": "user123",
  "exp": [some future timestamp]
}


## 🧪 Running Tests
To run unit and integration tests:

```bash
dotnet test
````

---

#### 🧰 3. Mockoon Setup

````markdown
## 🧰 Mockoon Stub Server

1. Install Mockoon: [https://mockoon.com](https://mockoon.com)
2. Import `Mockoon_environment.json` (provided in this repo, in root folder)
3. Start the server on port `3002`
4. It should respond to: `GET /api/meta/{operation}` with a description.

Example response:

```json
{
  "description": "This operation performs {{routeParams.operation}} on two numbers."
}
```
````

postman collection is in `MathOperationsAPI.postman_collection.json` (provided in this repo, in root folder)
